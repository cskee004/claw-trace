{
  "name": "openclaw-clawtrace",
  "version": "0.1.0",
  "description": "OpenClaw plugin that sends traces to ClawTrace via OTLP",
  "main": "index.js",
  "keywords": ["openclaw", "clawtrace", "otlp", "observability"],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "openclaw": {
    "extensions": ["./index.js"],
    "env": {
      "CLAWTRACE_ENDPOINT": {
        "description": "ClawTrace base URL to send OTLP traces to",
        "default": "http://localhost:3000"
      },
      "CLAWTRACE_ENABLED": {
        "description": "Set to false to disable tracing without uninstalling",
        "default": "true"
      }
    }
  }
}
